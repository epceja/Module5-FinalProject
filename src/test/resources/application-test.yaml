spring.cache.type: none
spring:
  init:
    mode: always
  datasource:
    #url: jdbc:postgresql://localhost:5433/jira-test
    #La siguiente línea es para que corran las pruebas. No es lo que debe ser.
    url: jdbc:postgresql://localhost:5432/jira
    username: jira
    password: CodeGymJira